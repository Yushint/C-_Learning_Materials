/*using System;

class FOREACH
{
    static void Main()
    {
        int[] nums = { 1, 2, 3, 4, 5 };
        char[] symbs = { 'A', 'B', 'C', 'D', 'E' };
        string[] txt = new string[] { "one", "two", "three" };

        Console.Title = "Массивы:";

        Console.WriteLine("Массив \"nums\": ");
        foreach (int num in nums)
        {
            Console.Write(num + " ");
        }

        Console.WriteLine("\nМассив \"symbs\": ");
        foreach (char symbol in symbs)
        {
            Console.Write(symbol + " ");
        }

        Console.WriteLine("\nМассив \"txt\": ");
        foreach (string s in txt)
        {
            Console.Write(s + " ");
        }
    }
}*/