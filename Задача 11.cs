/*using System;

class Array
{
    static void Main()
    {
        int number;
        int[] nums;

        Console.Title = "Введите размерность массива...";
        Console.Write("Размерность массива: ");

        number = Int32.Parse(Console.ReadLine());

        nums = new int[number];
        for (int i = 1; i < number; i++)
        {
            if (i % 5 == 2) nums[i] = i;
        }

        foreach (int num in nums) Console.Write(num + " ");
    }
}*/